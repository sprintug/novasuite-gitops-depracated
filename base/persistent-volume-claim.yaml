apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-hms
  namespace: argocd
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  volumeName: postgres-pv-hms
  storageClassName: standard
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-novasuite-backend
  namespace: argocd
  labels:
    type: local
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  volumeName: postgres-pv-novasuite-backend
  storageClassName: standard